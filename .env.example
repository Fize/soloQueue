# SoloQueue Environment Configuration
# Copy this file to .env and fill in your values

# ========== LLM Configuration ==========
# For DeepSeek R1:
OPENAI_API_KEY=sk-your-deepseek-api-key
OPENAI_BASE_URL=https://api.deepseek.com/v1
DEFAULT_MODEL=deepseek-reasoner

# For OpenAI (uncomment and modify):
# OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_BASE_URL=
# DEFAULT_MODEL=gpt-4o

# ========== Embedding Configuration ==========
# Enable semantic memory (L3 layer) with vector embeddings
# Set to true to enable semantic search and knowledge storage
SOLOQUEUE_EMBEDDING_ENABLED=false

# ========== Embedding Provider Options ==========
# Choose ONE of the following provider configurations:

# Option 1: Ollama (Local deployment, recommended for development)
# Requires: ollama serve + ollama pull nomic-embed-text
# SOLOQUEUE_EMBEDDING_PROVIDER=ollama
# SOLOQUEUE_EMBEDDING_MODEL=nomic-embed-text
# SOLOQUEUE_EMBEDDING_API_BASE=http://localhost:11434/v1
# SOLOQUEUE_EMBEDDING_DIMENSION=768

# Option 2: OpenAI (Cloud API, high quality)
# Uses the same OPENAI_API_KEY as above
# SOLOQUEUE_EMBEDDING_PROVIDER=openai
# SOLOQUEUE_EMBEDDING_MODEL=text-embedding-3-small

# Option 3: GLM (Chinese provider, OpenAI-compatible)
# SOLOQUEUE_EMBEDDING_PROVIDER=openai-compatible
# SOLOQUEUE_EMBEDDING_MODEL=embedding-2
# SOLOQUEUE_EMBEDDING_API_BASE=https://open.bigmodel.cn/api/paas/v4
# SOLOQUEUE_EMBEDDING_API_KEY=your-glm-api-key
# SOLOQUEUE_EMBEDDING_DIMENSION=1024

# Option 4: DeepSeek Embedding (if available)
# SOLOQUEUE_EMBEDDING_PROVIDER=openai-compatible
# SOLOQUEUE_EMBEDDING_MODEL=deepseek-embedding
# SOLOQUEUE_EMBEDDING_API_BASE=https://api.deepseek.com/v1
# SOLOQUEUE_EMBEDDING_API_KEY=sk-your-deepseek-api-key
# SOLOQUEUE_EMBEDDING_DIMENSION=1536

# Option 5: Moonshot/Kimi (Chinese provider)
# SOLOQUEUE_EMBEDDING_PROVIDER=openai-compatible
# SOLOQUEUE_EMBEDDING_MODEL=moonshot-embedding-v1
# SOLOQUEUE_EMBEDDING_API_BASE=https://api.moonshot.cn/v1
# SOLOQUEUE_EMBEDDING_API_KEY=your-moonshot-api-key
# SOLOQUEUE_EMBEDDING_DIMENSION=1536

# ========== System Configuration ==========
# Project root directory (optional, defaults to cwd)
# PROJECT_ROOT=/path/to/your/project

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Require user approval for dangerous operations (bash, write_file)
REQUIRE_APPROVAL=true
